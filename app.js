const grid = { 
        gridspaces: ["grid1", "grid2", "grid3", "grid4", "grid5", "grid6", "grid7", "grid8", "grid9"]
}

const playerOne = () => {
    const score = "";
    const gridplaces = {
        playergrid: []
}
}

function playerMoves(placement) {
    playerOne.playergrid.push(placement)
    console.log(playerOne.playergrid)
}


